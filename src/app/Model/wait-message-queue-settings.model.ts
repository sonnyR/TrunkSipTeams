export class WaitMessageQueueSettings {
    enabled: boolean
    waitMode: string
    handlingTime: number
    defaultHandlingTime: number
    queuePosition: number
    highVolumeMessageEnabled: boolean
    estimatedWaitingTime: number
    playUpdatedEstimatedWaitMessage: boolean
}
